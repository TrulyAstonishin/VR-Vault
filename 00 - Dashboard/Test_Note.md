The Mitochondria is the powerhouse of the cell.

There are 24 hours in a day.

The Japanese language is called 日本語

To print out a file into a Linux Terminal you use the command Cat

To feed an output as an input to another command you use a Pipe (|)

## Flashcards
#flashcards
Cat :: To print out a file in the Linux terminal
Pipe (|) :: To feed the output from one command into another